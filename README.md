# word-search.sh
Simple program to search for certain words in a text file (in this case I've got my own names.txt file). This then saves the matching words into a file (found-words.txt). Pretty simple but I like the result
